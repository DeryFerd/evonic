{
    "id": "mcp_call",
    "name": "MCP Call",
    "description": "Call a tool on a configured MCP server.",
    "function": {
        "name": "mcp_call",
        "description": "Invoke a tool on an external MCP server configured on this platform (e.g. run Claude Code's Bash tool via the 'claude-code' server). Call mcp_list_tools first to discover valid server names, tool names and each tool's input schema, then pass the tool's arguments as a JSON object. Requires the MCP Client plugin to be enabled.",
        "parameters": {
            "properties": {
                "server": {
                    "description": "Name of a configured MCP server (e.g. 'claude-code'). Discover valid names via mcp_list_tools.",
                    "type": "string"
                },
                "tool": {
                    "description": "Name of the tool to invoke on the server (e.g. 'Bash'), exactly as returned by mcp_list_tools.",
                    "type": "string"
                },
                "arguments": {
                    "description": "Arguments for the tool as a JSON object, matching the tool's input_schema from mcp_list_tools.",
                    "type": "object"
                },
                "timeout_sec": {
                    "description": "Optional timeout in seconds for this call. Capped at the admin-configured maximum.",
                    "type": "number"
                }
            },
            "required": [
                "server",
                "tool",
                "arguments"
            ],
            "type": "object"
        }
    },
    "mock_response": "{\"result\":\"hello-from-mcp\",\"server\":\"claude-code\",\"tool\":\"Bash\"}",
    "mock_response_type": "json",
    "created_at": "2026-07-03T00:00:00.000000",
    "updated_at": "2026-07-03T00:00:00.000000"
}
